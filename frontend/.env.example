# In dev, leave unset — Vite proxies /api to http://localhost:5000
# In production (Vercel), set this to your deployed backend's API URL, e.g.:
VITE_API_URL=https://oneseven17-backend.onrender.com/api
